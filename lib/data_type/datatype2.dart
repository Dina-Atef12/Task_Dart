void main() {
  //int y;
  // print(y);
  // it give me error because its non nullable local variable y must be assigned before it can be used .
  // to solve this error we can put '?' before variable
  int? n;
  print(n);
  // it output null.
}
